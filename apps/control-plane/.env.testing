# Committed on purpose: with APP_ENV=testing (phpunit.xml), Laravel loads
# THIS file instead of .env. Without it, environments with no .env (CI)
# trigger a dotenv file_get_contents warning that PHPUnit 12 surfaces as a
# test WARNING on every test. All real test configuration lives in
# phpunit.xml <env>; nothing here may contain secrets.
